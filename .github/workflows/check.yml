name: price check

# Meant to run every 5 minutes and scrape whatever is due: never checked, "check now" pressed in the
# app, or last checked more than CHECK_INTERVAL_MINUTES ago (default 5, so every item, every run).
# Runs with nothing due exit in ~20s. (Public repo: Actions minutes are free.)
#
# GitHub's schedule is best-effort and on this repo it has fired only every few hours regardless of
# the cron line. For a real 5-minute cadence, setup_trigger.py installs a pg_cron job in Supabase that
# calls workflow_dispatch every 5 minutes (see README "Making it actually run every 5 minutes").
# Keep the minute list in sync with CRON_MINUTES in pages.yml; the app uses it to show "next check in".
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

concurrency:
  group: pricewatch
  cancel-in-progress: false

permissions:
  contents: write         # only used in SQLite mode (no DATABASE_URL) to commit the DB file

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}       # Supabase; empty -> SQLite file in the repo
      CHECK_INTERVAL_MINUTES: ${{ vars.CHECK_INTERVAL_MINUTES || '5' }}
      CHECK_INTERVAL_HOURS: ${{ vars.CHECK_INTERVAL_HOURS }}   # legacy override, normally empty
      MIN_DROP_PCT: ${{ vars.MIN_DROP_PCT || '1' }}
      ATTENTION_COOLDOWN_HOURS: ${{ vars.ATTENTION_COOLDOWN_HOURS || '24' }}
      APP_URL: ${{ vars.APP_URL }}
      TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}   # personal-mode fallback only
      NOTIFIER: ${{ vars.NOTIFIER || 'telegram' }}
      NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
      TWILIO_SID: ${{ secrets.TWILIO_SID }}
      TWILIO_TOKEN: ${{ secrets.TWILIO_TOKEN }}
      TWILIO_FROM: ${{ secrets.TWILIO_FROM }}
      MY_PHONE: ${{ secrets.MY_PHONE }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt

      - name: Anything due?
        id: due
        run: echo "count=$(python check.py --due-count)" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        if: steps.due.outputs.count != '0'
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('requirements.txt') }}
      - if: steps.due.outputs.count != '0'
        run: playwright install --with-deps chromium

      - name: Check prices
        # runs even with 0 due so Telegram "connect" links still get processed
        run: python check.py --due

      - name: Commit price history (SQLite mode only)
        if: env.DATABASE_URL == ''
        run: |
          git config user.name "pricewatch bot"
          git config user.email "pricewatch@users.noreply.github.com"
          git add data/pricewatch.db
          if git diff --cached --quiet; then echo "no changes"; else
            git commit -m "price check $(date -u +'%Y-%m-%d %H:%M UTC')"
            git pull --rebase -X theirs || true
            git push
          fi
